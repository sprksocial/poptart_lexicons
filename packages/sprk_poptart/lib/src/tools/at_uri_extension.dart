// coverage:ignore-file
// GENERATED CODE - DO NOT MODIFY BY HAND
// ignore_for_file: type=lint
// ignore_for_file: unused_element, unused_import, duplicate_import, unnecessary_cast, deprecated_member_use, deprecated_member_use_from_same_package, use_function_type_syntax_for_parameters, unnecessary_const, avoid_init_to_null, invalid_override_different_default_values_named, prefer_expression_function_bodies, annotate_overrides, invalid_annotation_target, unnecessary_question_mark

import 'package:poptart_core/poptart_core.dart' show AtUri;

// **************************************************************************
// LexGenerator
// **************************************************************************

extension AtUriExtension on AtUri {
  /// Returns true if this uri is `so.sprk.actor.profile`, otherwise false.
  bool get isActorProfile => collection.toString() == 'so.sprk.actor.profile';

  /// Returns true if this uri is not `so.sprk.actor.profile`, otherwise false.
  bool get isNotActorProfile => !isActorProfile;

  /// Returns true if this uri is `so.sprk.feed.generator`, otherwise false.
  bool get isFeedGenerator => collection.toString() == 'so.sprk.feed.generator';

  /// Returns true if this uri is not `so.sprk.feed.generator`, otherwise false.
  bool get isNotFeedGenerator => !isFeedGenerator;

  /// Returns true if this uri is `so.sprk.feed.like`, otherwise false.
  bool get isFeedLike => collection.toString() == 'so.sprk.feed.like';

  /// Returns true if this uri is not `so.sprk.feed.like`, otherwise false.
  bool get isNotFeedLike => !isFeedLike;

  /// Returns true if this uri is `so.sprk.feed.post`, otherwise false.
  bool get isFeedPost => collection.toString() == 'so.sprk.feed.post';

  /// Returns true if this uri is not `so.sprk.feed.post`, otherwise false.
  bool get isNotFeedPost => !isFeedPost;

  /// Returns true if this uri is `so.sprk.feed.reply`, otherwise false.
  bool get isFeedReply => collection.toString() == 'so.sprk.feed.reply';

  /// Returns true if this uri is not `so.sprk.feed.reply`, otherwise false.
  bool get isNotFeedReply => !isFeedReply;

  /// Returns true if this uri is `so.sprk.feed.repost`, otherwise false.
  bool get isFeedRepost => collection.toString() == 'so.sprk.feed.repost';

  /// Returns true if this uri is not `so.sprk.feed.repost`, otherwise false.
  bool get isNotFeedRepost => !isFeedRepost;

  /// Returns true if this uri is `so.sprk.feed.threadgate`, otherwise false.
  bool get isFeedThreadgate =>
      collection.toString() == 'so.sprk.feed.threadgate';

  /// Returns true if this uri is not `so.sprk.feed.threadgate`, otherwise false.
  bool get isNotFeedThreadgate => !isFeedThreadgate;

  /// Returns true if this uri is `so.sprk.graph.block`, otherwise false.
  bool get isGraphBlock => collection.toString() == 'so.sprk.graph.block';

  /// Returns true if this uri is not `so.sprk.graph.block`, otherwise false.
  bool get isNotGraphBlock => !isGraphBlock;

  /// Returns true if this uri is `so.sprk.graph.follow`, otherwise false.
  bool get isGraphFollow => collection.toString() == 'so.sprk.graph.follow';

  /// Returns true if this uri is not `so.sprk.graph.follow`, otherwise false.
  bool get isNotGraphFollow => !isGraphFollow;

  /// Returns true if this uri is `so.sprk.labeler.service`, otherwise false.
  bool get isLabelerService =>
      collection.toString() == 'so.sprk.labeler.service';

  /// Returns true if this uri is not `so.sprk.labeler.service`, otherwise false.
  bool get isNotLabelerService => !isLabelerService;

  /// Returns true if this uri is `so.sprk.sound.audio`, otherwise false.
  bool get isSoundAudio => collection.toString() == 'so.sprk.sound.audio';

  /// Returns true if this uri is not `so.sprk.sound.audio`, otherwise false.
  bool get isNotSoundAudio => !isSoundAudio;

  /// Returns true if this uri is `so.sprk.story.post`, otherwise false.
  bool get isStoryPost => collection.toString() == 'so.sprk.story.post';

  /// Returns true if this uri is not `so.sprk.story.post`, otherwise false.
  bool get isNotStoryPost => !isStoryPost;
}
