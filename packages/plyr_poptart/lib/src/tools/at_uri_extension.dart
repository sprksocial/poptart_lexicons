// coverage:ignore-file
// GENERATED CODE - DO NOT MODIFY BY HAND
// ignore_for_file: type=lint
// ignore_for_file: unused_element, unused_import, duplicate_import, unnecessary_cast, deprecated_member_use, deprecated_member_use_from_same_package, use_function_type_syntax_for_parameters, unnecessary_const, avoid_init_to_null, invalid_override_different_default_values_named, prefer_expression_function_bodies, annotate_overrides, invalid_annotation_target, unnecessary_question_mark

import 'package:poptart_core/poptart_core.dart' show AtUri;

// **************************************************************************
// LexGenerator
// **************************************************************************

extension AtUriExtension on AtUri {
  /// Returns true if this uri is `fm.plyr.comment`, otherwise false.
  bool get isComment => collection.toString() == 'fm.plyr.comment';

  /// Returns true if this uri is not `fm.plyr.comment`, otherwise false.
  bool get isNotComment => !isComment;

  /// Returns true if this uri is `fm.plyr.like`, otherwise false.
  bool get isLike => collection.toString() == 'fm.plyr.like';

  /// Returns true if this uri is not `fm.plyr.like`, otherwise false.
  bool get isNotLike => !isLike;

  /// Returns true if this uri is `fm.plyr.list`, otherwise false.
  bool get isList => collection.toString() == 'fm.plyr.list';

  /// Returns true if this uri is not `fm.plyr.list`, otherwise false.
  bool get isNotList => !isList;

  /// Returns true if this uri is `fm.plyr.actor.profile`, otherwise false.
  bool get isActorProfile => collection.toString() == 'fm.plyr.actor.profile';

  /// Returns true if this uri is not `fm.plyr.actor.profile`, otherwise false.
  bool get isNotActorProfile => !isActorProfile;

  /// Returns true if this uri is `fm.plyr.track`, otherwise false.
  bool get isTrack => collection.toString() == 'fm.plyr.track';

  /// Returns true if this uri is not `fm.plyr.track`, otherwise false.
  bool get isNotTrack => !isTrack;
}
