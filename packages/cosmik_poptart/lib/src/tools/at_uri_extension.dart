// coverage:ignore-file
// GENERATED CODE - DO NOT MODIFY BY HAND
// ignore_for_file: type=lint
// ignore_for_file: unused_element, unused_import, duplicate_import, unnecessary_cast, deprecated_member_use, deprecated_member_use_from_same_package, use_function_type_syntax_for_parameters, unnecessary_const, avoid_init_to_null, invalid_override_different_default_values_named, prefer_expression_function_bodies, annotate_overrides, invalid_annotation_target, unnecessary_question_mark

import 'package:poptart_core/poptart_core.dart' show AtUri;

// **************************************************************************
// LexGenerator
// **************************************************************************

extension AtUriExtension on AtUri {
  /// Returns true if this uri is `network.cosmik.card`, otherwise false.
  bool get isCard => collection.toString() == 'network.cosmik.card';

  /// Returns true if this uri is not `network.cosmik.card`, otherwise false.
  bool get isNotCard => !isCard;

  /// Returns true if this uri is `network.cosmik.collection`, otherwise false.
  bool get isCollection => collection.toString() == 'network.cosmik.collection';

  /// Returns true if this uri is not `network.cosmik.collection`, otherwise false.
  bool get isNotCollection => !isCollection;

  /// Returns true if this uri is `network.cosmik.collectionLink`, otherwise false.
  bool get isCollectionLink =>
      collection.toString() == 'network.cosmik.collectionLink';

  /// Returns true if this uri is not `network.cosmik.collectionLink`, otherwise false.
  bool get isNotCollectionLink => !isCollectionLink;

  /// Returns true if this uri is `network.cosmik.collectionLinkRemoval`, otherwise false.
  bool get isCollectionLinkRemoval =>
      collection.toString() == 'network.cosmik.collectionLinkRemoval';

  /// Returns true if this uri is not `network.cosmik.collectionLinkRemoval`, otherwise false.
  bool get isNotCollectionLinkRemoval => !isCollectionLinkRemoval;

  /// Returns true if this uri is `network.cosmik.connection`, otherwise false.
  bool get isConnection => collection.toString() == 'network.cosmik.connection';

  /// Returns true if this uri is not `network.cosmik.connection`, otherwise false.
  bool get isNotConnection => !isConnection;

  /// Returns true if this uri is `network.cosmik.follow`, otherwise false.
  bool get isFollow => collection.toString() == 'network.cosmik.follow';

  /// Returns true if this uri is not `network.cosmik.follow`, otherwise false.
  bool get isNotFollow => !isFollow;
}
