// coverage:ignore-file
// GENERATED CODE - DO NOT MODIFY BY HAND
// ignore_for_file: type=lint
// ignore_for_file: unused_element, unused_import, duplicate_import, unnecessary_cast, deprecated_member_use, deprecated_member_use_from_same_package, use_function_type_syntax_for_parameters, unnecessary_const, avoid_init_to_null, invalid_override_different_default_values_named, prefer_expression_function_bodies, annotate_overrides, invalid_annotation_target, unnecessary_question_mark

import 'package:poptart_core/poptart_core.dart' show AtUri;

// **************************************************************************
// LexGenerator
// **************************************************************************

extension AtUriExtension on AtUri {
  /// Returns true if this uri is `at.margin.apikey`, otherwise false.
  bool get isApikey => collection.toString() == 'at.margin.apikey';

  /// Returns true if this uri is not `at.margin.apikey`, otherwise false.
  bool get isNotApikey => !isApikey;

  /// Returns true if this uri is `at.margin.collection`, otherwise false.
  bool get isCollection => collection.toString() == 'at.margin.collection';

  /// Returns true if this uri is not `at.margin.collection`, otherwise false.
  bool get isNotCollection => !isCollection;

  /// Returns true if this uri is `at.margin.collectionItem`, otherwise false.
  bool get isCollectionItem =>
      collection.toString() == 'at.margin.collectionItem';

  /// Returns true if this uri is not `at.margin.collectionItem`, otherwise false.
  bool get isNotCollectionItem => !isCollectionItem;

  /// Returns true if this uri is `at.margin.like`, otherwise false.
  bool get isLike => collection.toString() == 'at.margin.like';

  /// Returns true if this uri is not `at.margin.like`, otherwise false.
  bool get isNotLike => !isLike;

  /// Returns true if this uri is `at.margin.note`, otherwise false.
  bool get isNote => collection.toString() == 'at.margin.note';

  /// Returns true if this uri is not `at.margin.note`, otherwise false.
  bool get isNotNote => !isNote;

  /// Returns true if this uri is `at.margin.preferences`, otherwise false.
  bool get isPreferences => collection.toString() == 'at.margin.preferences';

  /// Returns true if this uri is not `at.margin.preferences`, otherwise false.
  bool get isNotPreferences => !isPreferences;

  /// Returns true if this uri is `at.margin.profile`, otherwise false.
  bool get isProfile => collection.toString() == 'at.margin.profile';

  /// Returns true if this uri is not `at.margin.profile`, otherwise false.
  bool get isNotProfile => !isProfile;

  /// Returns true if this uri is `at.margin.reply`, otherwise false.
  bool get isReply => collection.toString() == 'at.margin.reply';

  /// Returns true if this uri is not `at.margin.reply`, otherwise false.
  bool get isNotReply => !isReply;
}
