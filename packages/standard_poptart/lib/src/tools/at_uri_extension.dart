// coverage:ignore-file
// GENERATED CODE - DO NOT MODIFY BY HAND
// ignore_for_file: type=lint
// ignore_for_file: unused_element, unused_import, duplicate_import, unnecessary_cast, deprecated_member_use, deprecated_member_use_from_same_package, use_function_type_syntax_for_parameters, unnecessary_const, avoid_init_to_null, invalid_override_different_default_values_named, prefer_expression_function_bodies, annotate_overrides, invalid_annotation_target, unnecessary_question_mark

import 'package:poptart_core/poptart_core.dart' show AtUri;

// **************************************************************************
// LexGenerator
// **************************************************************************

extension AtUriExtension on AtUri {
  /// Returns true if this uri is `site.standard.document`, otherwise false.
  bool get isDocument => collection.toString() == 'site.standard.document';

  /// Returns true if this uri is not `site.standard.document`, otherwise false.
  bool get isNotDocument => !isDocument;

  /// Returns true if this uri is `site.standard.graph.recommend`, otherwise false.
  bool get isGraphRecommend =>
      collection.toString() == 'site.standard.graph.recommend';

  /// Returns true if this uri is not `site.standard.graph.recommend`, otherwise false.
  bool get isNotGraphRecommend => !isGraphRecommend;

  /// Returns true if this uri is `site.standard.graph.subscription`, otherwise false.
  bool get isGraphSubscription =>
      collection.toString() == 'site.standard.graph.subscription';

  /// Returns true if this uri is not `site.standard.graph.subscription`, otherwise false.
  bool get isNotGraphSubscription => !isGraphSubscription;

  /// Returns true if this uri is `site.standard.publication`, otherwise false.
  bool get isPublication =>
      collection.toString() == 'site.standard.publication';

  /// Returns true if this uri is not `site.standard.publication`, otherwise false.
  bool get isNotPublication => !isPublication;

  /// Returns true if this uri is `site.standard.theme.basic`, otherwise false.
  bool get isThemeBasic => collection.toString() == 'site.standard.theme.basic';

  /// Returns true if this uri is not `site.standard.theme.basic`, otherwise false.
  bool get isNotThemeBasic => !isThemeBasic;
}
